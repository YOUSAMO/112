<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.member.repository.UserLikeRepository">

    <insert id="insertLike" parameterType="com.example.member.entity.UserLike">
        INSERT INTO users_like (user_id, board_id, board_type, liked_at)
        VALUES (#{userId}, #{boardId}, #{boardType}, NOW())
    </insert>

    <delete id="deleteLike">
        DELETE FROM users_like
        WHERE user_id = #{userId} AND board_id = #{boardId} AND board_type = #{boardType}
    </delete>

    <select id="findLike" resultType="com.example.member.entity.UserLike">
        SELECT like_id, user_id, board_id, board_type, liked_at
        FROM users_like
        WHERE user_id = #{userId} AND board_id = #{boardId} AND board_type = #{boardType}
    </select>

</mapper>